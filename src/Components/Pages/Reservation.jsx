import React from 'react';
import Reservation_Section from '../Reservation/Reservation_Section';

const Reservation = () => {
    return (
        <div>
         <Reservation_Section/>
        </div>
    );
};

export default Reservation;